# Coherence Engine (substrate)

This package implements a minimal, in-memory coherence engine used as a substrate building block.

Exports:
- init(config)
- createField(name, opts)
- getField(id)
- listFields()
- registerOperator(name, fn)
- runOperator(name, ...args)

This is a development stub; replace with full implementation as needed.
