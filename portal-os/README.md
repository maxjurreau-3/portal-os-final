# Portal OS

This directory contains the Portal‑OS web application (Vite + React). It is intended to be built independently when the repository is not used as an npm workspace.

Start locally:

```bash
cd portal-os
npm ci
npm run dev
```
